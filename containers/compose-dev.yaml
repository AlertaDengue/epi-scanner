services:
  wave:
    command: ["wave", "run", "--no-autostart", "epi_scanner.app"]
    volumes:
      - ${HOST_EPISCANNER_ROOT}:${PROJECT_ROOT}
      - /opt/services/epi-scanner/.venv
    networks:
      - infodengue-prod_infodengue

networks:
  infodengue-prod_infodengue:
    external: True
